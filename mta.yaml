_schema-version: 3.3.0
ID: apmorrowland
description: A simple CAP project.
version: 1.0.0
modules:
- name: apmorrowland-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: apmorrowland-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: apmorrowland-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: apmorrowland-db
  parameters:
    buildpack: nodejs_buildpack
- name: apmorrowland-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: apmorrowland-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - myapmorrowlandordersappordersapp.zip
      name: myapmorrowlandordersappordersapp
      target-path: resources/
    - artifacts:
      - myapmorrowlandordersapp.zip
      name: myapmorrowlandordersapp
      target-path: resources/
    - artifacts:
      - myapmorrowlandlineupapp.zip
      name: myapmorrowlandlineupapp
      target-path: resources/
    - artifacts:
      - myapmorrowlandleaderboardapp.zip
      name: myapmorrowlandleaderboardapp
      target-path: resources/
- name: myapmorrowlandordersappordersapp
  type: html5
  path: app/ordersapp
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: myapmorrowlandordersapp
  type: html5
  path: app/ordersapp
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: myapmorrowlandlineupapp
  type: html5
  path: app/lineupapp
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: myapmorrowlandleaderboardapp
  type: html5
  path: app/leaderboardapp
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: apmorrowland-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: apmorrowland-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: apmorrowland-html5-service
    service-plan: app-host
- name: apmorrowland-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: apmorrowland-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
